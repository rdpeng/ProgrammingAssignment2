## Put comments here that give an overall description of what your
## functions do

## Write a short comment describing this function

makeCacheMatrix <- function(x = matrix()) {

}


## Write a short comment describing this function

cacheSolve <- function(x, ...) {
        ## Return a matrix that is the inverse of 'x'
}

## This function has the purpose to show a contained inverse matrix.
makeCacheMatrix <- function(x = matrix( c(1:3)), nrow=2, ncol=2) {}

        ## This function returns the inverse of the makeCacheMatrix.
        cacheSolve = makeCacheMatrix^-1