
## This Program is used to compute the inverse of square matrix
cacheSolve <- function(x, ...) {
        ## Return a matrix that is the inverse of 'x'
        solve(x)        # solve function will give the inverse of matrix.
}
