library(datasets)
data(iris)
iris
x <- iris$Sepal.Length
x
makeVector <- function(x = numeric()) {
m <- NULL
set <- function(y) {
x <<- y
m <<- NULL
}
get <- function() x
setmean <- function(mean) m <<- mean
getmean <- function() m
list(set = set, get = get,
setmean = setmean,
getmean = getmean)
}
cachemean <- function(x, ...) {
m <- x$getmean()
if(!is.null(m)) {
message("getting cached data")
return(m)
}
data <- x$get()
m <- mean(data, ...)
x$setmean(m)

m
}
makeVector(x)
cachemean(x)
check <- makeVector(x)
cachemean(check)
cachemean(check)
check
x
class(x)
check
class(check)
m
mean(x)
check <- check$set(iris$Sepal.Width)
mean(check)
cashemean(check)
cachemean(check)
iris$Sepal.Width
check$get
check <- makeVector(x)
check$set
y <- Sepal.Width
y <- iris$Sepal.Width
check$set(y)
check
cachemean(check)
cachemean(check)
library(datasets)
data("iris")
iris
x <- iris$Sepal.Length
makeVector <- function(x = numeric()) {
m <- NULL
set <- function(y) {
x <<- y
m <<- NULL
}
get <- function() x
setmean <- function(mean) m <<- mean
getmean <- function() m
list(set = set, get = get,
setmean = setmean,
getmean = getmean)
}
cachemean <- function(x, ...) {
m <- x$getmean()
if(!is.null(m)) {
message("getting cached data")
return(m)
}
data <- x$get()
m <- mean(data, ...)
x$setmean(m)
m
}
check <- makeVector(x)
check
cachmean(check)
cachmean(check)
class(x)
class(check)
class(x)
check$setmean()
x
check <- makeVector(x)
check$setmean()
check$getmean()
cachmean(x)
class(cachmean)
class(cachemean)
cachemean(x)
cachemean(check)
check$getmean()
